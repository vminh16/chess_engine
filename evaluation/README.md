# Evaluation Layer

This folder bridges the engine and the evaluators.

- `static_eval.py`: handcrafted/static score components
- `nn.py`: legacy neural evaluator wrapper
- `naive.py`: simple baseline evaluation utilities

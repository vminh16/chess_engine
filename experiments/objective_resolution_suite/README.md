# Objective Resolution Suite

Purpose: compare the L-family objective variants and determine the best checkpoint to carry forward.

Main entry points:
- `objective_resolution_suite.ipynb`
- `objective_resolution_helpers.py`
- `outputs/reports/`

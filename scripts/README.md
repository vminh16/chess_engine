# Utility Scripts

Small utility scripts that do not fit inside the main train/search packages.

- `play_cli.py`: command-line play helper
- `Quantization.py`: quantization experiments
- `test_negamax.py`: local search sanity checks
- `scratch/`: temporary or exploratory scripts moved out of the repo root

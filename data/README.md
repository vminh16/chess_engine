# Data Pipeline

Data preparation notebooks and scripts live here.

Key artifacts:
- `process_data.ipynb`: build the sharded dataset used by training and experiments
- `concat_data.py`, `processing_data.py`: supporting preprocessing scripts
- `process/`: train/val/test shards consumed by the training pipeline

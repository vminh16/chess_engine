# Model Architectures

This folder contains two model lines:

- `architecture/`: legacy model family used by the web/UCI integration
- `architecture_v2/`: research-oriented value network used in recent experiments

Use `architecture_v2/` as the current research baseline unless a document explicitly refers to the older stack.
